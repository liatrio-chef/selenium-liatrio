---
driver:
  name: docker
  use_sudo: false

platforms:
  - name: centos-7.2

suites:
  - name: default
    run_list:
      - recipe[java]
      - recipe[selenium-liatrio::default]
    attributes:
      java:
        jdk_version: "8"
